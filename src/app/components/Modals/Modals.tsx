import { ConfigModal } from './ConfigModal';
import { PresetsModal } from './PresetsModal';

export const Modals = () => {
  return (
    <>
      <ConfigModal />
      <PresetsModal />
    </>
  );
};
