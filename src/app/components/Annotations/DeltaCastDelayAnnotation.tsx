import styled from 'styled-components';

const StyledDiv = styled.div``;

export const DeltaCastDelayAnnotation = () => {
  // const { config } = useConfig();
  //
  return <StyledDiv data-name="DeltaCastDelayAnnotation"></StyledDiv>;
};
