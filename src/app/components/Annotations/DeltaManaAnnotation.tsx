import styled from 'styled-components';

const StyledDiv = styled.div``;

export const DeltaManaAnnotation = () => {
  // const { config } = useConfig();
  //
  return <StyledDiv data-name="DeltaManaAnnotation"></StyledDiv>;
};
